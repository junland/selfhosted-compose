backend home
    balance roundrobin
    mode http
    option forwardfor

    http-request add-header X-Forwarded-Proto https if { ssl_fc }    
    http-request add-header X-Forwarded-For %[src]

    server s1 hass:8123 check resolvers container_dns resolve-prefer ipv4 inter 3s rise 3 fall 3
