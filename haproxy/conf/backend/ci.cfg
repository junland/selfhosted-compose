backend ci
    balance roundrobin
    mode http

    http-request cache-use default_cache
    http-response cache-store default_cache

    server s1 jenkins:8080 check resolvers container_dns resolve-prefer ipv4
