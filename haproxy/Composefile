services:
  haproxy:
    build:
      context: .
      dockerfile: Containerfile
    image: localhost/haproxy:latest
    container_name: haproxy-web-service
    hostname: 'haproxy'
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    environment:
      TZ: ${SELFHOSTED_TZ}
    volumes:
      - ./conf:/var/lib/haproxy:ro
      - /var/run/haproxy:/var/run/haproxy:ro
      - '$SELFHOSTED_HAPROXY_EXTERNAL_CERTS_DIR:/etc/ssl/certs/external':ro
    command: haproxy -f haproxy.cfg -f backend
    labels:
      - "io.containers.autoupdate=local"
      - "com.infra.service=haproxy"
